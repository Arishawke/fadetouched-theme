# Fadetouched for Cava

Copy `fadetouched` to `~/.config/cava/themes/`, then set this in the `[color]` section of `~/.config/cava/config`:

```ini
theme = 'fadetouched'
```

The theme sets only Cava's colors and leaves audio and visualization settings unchanged.
