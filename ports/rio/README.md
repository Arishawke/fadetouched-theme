# Fadetouched for Rio

Copy `fadetouched.toml` to Rio's `themes` directory, then add this top-level setting to `config.toml`:

```toml
theme = "fadetouched"
```

Rio uses `~/.config/rio/themes/` on Linux and macOS, and `%USERPROFILE%\AppData\Local\rio\themes\` on Windows.
