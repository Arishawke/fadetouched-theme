# Fadetouched for btop

## Install

1. Copy [`fadetouched.theme`](fadetouched.theme) to
   `~/.config/btop/themes/fadetouched.theme`.
2. Open btop's **Options** and select **fadetouched** under `color_theme`.

If `XDG_CONFIG_HOME` is set, use `$XDG_CONFIG_HOME/btop/themes/` instead.
