## What

Briefly, what this changes.

## Checklist

- [ ] Colors changed only in `palette.json` (never hand-edited in a port)
- [ ] `npm run build` ran and generated files are committed
- [ ] `npm run contrast` passes
- [ ] `npm test` passes
- [ ] If adding a port: registered in `scripts/generate.mjs` and documented in `README.md`
